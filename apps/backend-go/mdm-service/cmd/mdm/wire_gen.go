// Code generated by Wire. DO NOT EDIT.

//go:generate go run -mod=mod github.com/google/wire/cmd/wire
//go:build !wireinject
// +build !wireinject

package main

import (
	"github.com/arda-labs/arda/arda-be-go/services/mdm-service/internal/biz"
	"github.com/arda-labs/arda/arda-be-go/services/mdm-service/internal/conf"
	"github.com/arda-labs/arda/arda-be-go/services/mdm-service/internal/data"
	"github.com/arda-labs/arda/arda-be-go/services/mdm-service/internal/server"
	"github.com/arda-labs/arda/arda-be-go/services/mdm-service/internal/service"
	"github.com/go-kratos/kratos/v2"
	"github.com/go-kratos/kratos/v2/log"
)

import (
	_ "go.uber.org/automaxprocs"
)

// Injectors from wire.go:

// wireApp init kratos application.
func wireApp(confServer *conf.Server, confData *conf.Data, jwt *conf.JWT, logger log.Logger) (*kratos.App, func(), error) {
	dataData, cleanup, err := data.NewData(confData, logger)
	if err != nil {
		return nil, nil, err
	}
	mdmRepo := data.NewMdmRepo(dataData, logger)
	mdmUsecase := biz.NewMdmUsecase(mdmRepo)
	mdmService := service.NewMdmService(mdmUsecase)
	grpcServer := server.NewGRPCServer(confServer, mdmService, logger)
	httpServer := server.NewHTTPServer(confServer, jwt, mdmService, logger)
	app := newApp(logger, grpcServer, httpServer)
	return app, func() {
		cleanup()
	}, nil
}
